# engine
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
asyncpg>=0.29

# ml
# Pinned, not floored. ml/model.joblib is a pickle of scikit-learn's own objects, so it can only
# be loaded by the version that wrote it - a mismatch makes Scorer.load()/predict() throw and
# the engine silently falls back to rules.py. Change these two only by retraining afterwards.
scikit-learn==1.9.1
joblib==1.4.2

# mentor voice (not wired up yet)
google-genai>=0.3
elevenlabs>=1.0
